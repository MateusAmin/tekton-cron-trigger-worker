Archived: This was toy to test binary sizes. Got this docker image down to 396KB.  

# tk-cron-worker

A worker run as a cron job that hits a Tekton trigger for cron'd pipeline runs 

```sh
docker build .
``` 
